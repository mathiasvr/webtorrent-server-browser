{
  "name": "webtorrent-server-browser",
  "version": "1.0.0",
  "description": "test `torrent.createServer()` using service worker",
  "private": true,
  "dependencies": {
    "matroska-subtitles": "github:mathiasvr/matroska-subtitles#cues"
  },
  "devDependencies": {
    "webpack": "^4.44.1",
    "webpack-cli": "^3.3.12"
  },
  "scripts": {
    "build-fix": "webpack --mode=development --resolve-alias ebml=ebml/lib/ebml.esm.js",
    "build": "webpack --mode=development",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
