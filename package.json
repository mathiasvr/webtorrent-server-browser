{
  "name": "webtorrent-server-browser",
  "version": "1.0.0",
  "description": "test `torrent.createServer()` using service worker",
  "private": true,
  "dependencies": {
    "matroska-subtitles": "^3.0.0",
    "webtorrent": "^0.108.6"
  },
  "devDependencies": {
    "webpack": "^4.44.2",
    "webpack-cli": "^3.3.12"
  },
  "scripts": {
    "build": "webpack --mode=development",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
